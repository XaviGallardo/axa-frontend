import styled from 'styled-components';

const GnomeProfessions = styled.div`
  p {
    font-size: 2rem;
    line-height: 1.5;
    margin: 0.5rem 0;
    font-weight: bold;
  }
  .professions-list {
    font-weight: normal;
  }
`;

export default GnomeProfessions;
